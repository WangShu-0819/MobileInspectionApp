# MobileInspectionApp 阶段 A 增量 UI 重构指令

## 任务性质

继续开发现有 `MobileInspectionApp`，这是对已经验收通过的阶段 A 工程进行**增量信息架构和视觉重构**，不是重新创建项目，也不是开始阶段 B。

工作目录：

`D:\study\Textile_defects\Wearable Inspection\MobileInspectionApp`

开始前完整阅读并核对：

1. `PHASE_A_SUMMARY.md`
2. `PHASE_A_REPORT.md`
3. 当前导航、Screen、Theme、ViewModel、Repository 和 Room 代码
4. 总需求：`D:\study\Textile_defects\Wearable Inspection\Wearable Inspection\MOBILE_INSPECTION_AGENT_INSTRUCTION.md`
5. 图标概念稿：`D:\study\Textile_defects\Wearable Inspection\Wearable Inspection\docs\app_icon_visual_inspection_concept.png`

MD 报告只表示历史执行记录，源码、构建结果、测试结果和真机状态才是事实。修改前先列出当前实现与目标的差异，以及计划保留、修改、新增的文件。

## 已完成内容：不要重复实施

根据最新 `PHASE_A_SUMMARY.md`，以下内容已经完成：

- 独立工程、Gradle Wrapper、命名空间和 `applicationId`
- 新旧 App 共存构建和安装
- `WorkbenchViewModelFactory` 启动闪退修复
- 10 次冷启动和清除数据启动验证
- App 中文名称“视觉质检”
- Room `exportSchema=true` 和移除 destructive migration
- Manifest 权限精简和移除 `largeHeap`
- 数据库与外键仪器测试文件

除非当前源码或复测证明存在回归，否则不要重做、删除或推翻这些实现。本任务不修改旧 `Wearable Inspection` 工程，不实现轮廓提取、ROI 算法、OCR 算法、Excel 或完整检测业务。

当前已知仍需处理：

- 当前仍是“工作台/记录/模板/设置”4 Tab。
- 当前仍是暗色主题。
- 当前工作台虽有“上实时、下模板”的占位结构，但还没有真正的手机实时预览、模板只读对照和轮廓对齐引导。
- 模板和设置仍是一级 Tab。
- `connectedAndroidTest` 在总结中仍标记为待实际执行。
- 现有图标背景使用渐变，与最新纯色视觉规范冲突。

## 1. 导航改成三个一级页面

底部导航只保留三个入口，顺序固定：

1. `现场采集`
2. `追溯记录`
3. `我的`

路由迁移：

- 原 `Workbench` 改为 `现场采集`。
- 原 `Records` 改为 `追溯记录`。
- 原 `Templates` 从底部导航移除，作为“我的 > 模板配置”二级页。
- 原 `Settings` 从底部导航移除，作为“我的 > 应用设置”二级页。

要求：

- 不删除现有模板页和设置页的可复用代码，只调整路由归属和页面外观。
- 三个 Tab 等宽，选中时不改变控件尺寸。
- 现场采集使用相机/取景图标，追溯记录使用历史/列表图标，我的使用用户图标。
- 选中项为深蓝图标和文字，配低对比度浅蓝选中底；未选中项为灰色。
- 底部导航内容高度约 `72dp`，正确处理系统导航栏 Insets。
- 每个 Tab 保留自己的导航栈、滚动位置和筛选状态。
- 二级页面隐藏底部导航，返回后回到原一级页和原位置。

## 2. 现场采集

现场采集是默认启动页，只负责现场检测，不允许在此编辑模板。核心布局必须是“上实时、下模板”：上方为手机相机实时预览和轮廓引导，下方为当前模板的只读参考图及检测准备信息。“模板配置移到我的”只表示编辑入口迁移，不能删除现场采集中的模板对照区。

内容顺序：

1. 顶部栏：标题“现场采集”；右侧放 DPM 扫码和钢印 OCR 图标按钮。
2. 浅蓝信息带：显示“离线可用”和相机/权限状态。
3. 当前零件：显示名称、型号、DPM 绑定状态及切换入口。
4. 上半区实时画面：CameraX 手机后置摄像头预览，约占可用内容高度的 `55%~65%`，叠加模板主体轮廓、ROI 边界和对齐提示。
5. 下半区当前模板：显示真实模板参考图、模板名称、轮廓预览、启用 ROI 数量和就绪状态，仅供对照，可放大查看但不可编辑。
6. 检测操作：显示相机和模板就绪状态，页面只保留一个深蓝实心拍照/开始检测主按钮。
7. 今日概览：通过、不通过、待复核三个紧凑指标。
8. 最近一次检测：缩略图、零件、时间、最终状态，点击进入追溯详情。

轮廓引导要求：

- 从当前模板读取主体轮廓及 ROI。
- 使用已有特征匹配/单应性方案估计实时画面中的模板姿态。
- 使用同一变换把模板轮廓和 ROI 投影到 CameraX 预览坐标。
- 正确处理传感器旋转、PreviewView 裁剪、缩放和屏幕坐标转换，禁止直接按屏幕宽高拉伸。
- 未对齐时使用白色或浅青色轮廓，并显示简短的左/右、上/下、远/近或旋转提示。
- 达到阈值时轮廓变绿并显示“已对齐，可拍摄”。
- 第一版允许未对齐时强制拍照，但必须二次确认并记录 `alignmentOverride=true`。
- 上方实时投影和下方模板参考必须来自同一个模板；切换零件/模板时原子更新，不得混用旧状态。

模板缺失时：

- 禁用“开始检测”。
- 明确显示缺少模板或 ROI 的原因。
- 提供“前往我的 > 模板配置”文字链接。
- 点击后切换到“我的”，滚动或聚焦到模板配置入口。
- 下方模板区域显示完整空状态，不隐藏整个模板区域。
- 页面不得出现“管理模板”、模板编辑、轮廓编辑或 ROI 编辑按钮。

阶段 A 尚无真实检测数据时使用真实空状态，不伪造通过率、照片或历史记录。

## 3. 追溯记录

追溯记录统一承载历史查询、复核和结果移交。

阶段 A 本次先完成稳定页面结构和状态，不实现阶段 D 的实际打包算法：

- 顶部标题“追溯记录”，右侧搜索按钮。
- 顶部提供通过、不通过、待复核的紧凑统计区。
- 支持零件、模板、状态和时间筛选的 UI；筛选使用 `ModalBottomSheet`。
- 生效的筛选显示数量，并可一键清除。
- 记录按日期分组，每条依次显示缩略图、零件、模板、时间、状态和进入箭头。
- 详情页面预留原图/识别图切换、ROI 明细、人工复核、备注和结果包导出位置。
- 当前无数据时显示设计完整的空状态，不显示不可用的假记录。
- 多选模式显示顶部操作栏，底部导航不能与批量操作栏重叠。
- 导出入口若尚未实现，明确标记为不可用状态或隐藏，不用 Toast 假装成功。

## 4. 我的

“我的”不是登录页，不虚构账号、头像、角色、工房或云同步状态。

顶部品牌区：

- 显示新版 App 图标。
- 名称“视觉质检”。
- 副标题“现场工作台 · 离线可用”。
- 布局紧凑，不使用营销式 Hero、大面积深蓝块或装饰背景。

功能分组顺序：

### 检测配置

1. `模板配置`：必须位于首屏第一项，显示 `X 个零件 · X 个模板 · X 个 ROI`；存在缺失配置时显示黄色 `X 项待完善`。
2. `零件管理`：创建、编辑、删除零件和 DPM 绑定。
3. `默认检测参数`：默认阈值及预处理配置入口。

### 数据管理

- 模板包导入/导出
- 结果包管理
- 存储空间
- 数据清理

### 应用设置

- 相机与图像质量
- 提示音与振动
- 权限与诊断

页面底部以次级文字显示版本号、构建日期和“数据仅保存在本机”。模板配置、零件管理和设置都属于“我的”模块，不得重新放回底部导航。

## 5. 模板配置二级页

入口层级：

`我的 -> 模板配置 -> 模板详情 -> 拍摄/导入 -> 轮廓提取 -> ROI 编辑`

本次只调整已有模板占位页的入口和视觉结构，不提前实现阶段 B 的图像和 ROI 逻辑。

模板条目未来需要承载：

- 真实样本缩略图
- 模板名称和所属零件
- ROI 数量
- 更新时间
- 启用状态

新增模板使用带相机/添加图标的 FAB。空状态提供明确的新建入口。不得使用嵌套卡片或大段说明文字。

## 6. 统一视觉系统

采用明亮、克制的工业台账风格，替换当前暗色主题。

颜色 Token：

- 页面背景：`#F3F7FA`
- 内容表面：`#FFFFFF`
- 顶部浅色信息带：`#E7F3F8`
- 主色/选中态：`#0F5B85`
- 深色强调：`#0A466B`
- 分隔线/描边：`#D8E3E9`
- 主文字：`#202A33`
- 次级文字：`#74808A`
- 通过：`#218657`
- 不通过：`#C44747`
- 待复核：`#B7791F`

不得只做颜色替换。要建立统一 Theme Token 和公共组件，页面禁止散落硬编码颜色。

尺寸 Token：

- 页面水平边距：`16dp`
- 页面区块间距：`20dp`
- 同组条目间距：`8dp`
- 常用内部间距：`8/12/16dp`
- 顶部应用栏：`56dp`
- 卡片圆角：最大 `8dp`
- 卡片边框：`1dp` 浅灰蓝
- 按钮和触控区域：至少 `48dp`
- 缩略图：`72dp` 或 `88dp`，圆角 `6dp`

字体 Token：

- 页面标题：`22sp` SemiBold
- 区块标题：`18sp` SemiBold
- 条目标题：`16sp` Medium/SemiBold
- 正文：`14sp`
- 辅助文字：`12sp`
- 重要统计数字：`20sp` SemiBold

禁止：

- 渐变、发光、装饰性光斑
- 大面积暗色背景
- 多层嵌套卡片
- 页面区块整体做成漂浮大卡片
- 超大标题和营销文案
- 过度圆角和重阴影
- 只用颜色表达状态
- 文字、图标或按钮随状态变化引发布局跳动

状态必须同时使用颜色、图标和文字：通过为绿色勾选，不通过为红色警示，待复核为黄色提示，异常为错误图标。

## 7. App 图标

以 `app_icon_visual_inspection_concept.png` 为构图参考，不直接把生成 PNG 原样作为最终 Adaptive Icon。

正式图标含义：

- 四个白色取景框角：手机相机与 ROI 定位。
- 中央浅青蓝圆形镜头：机器视觉检测。
- 右下绿色勾选：质检通过。
- 深蓝纯色背景：与 App 主色一致。

必须用 Android `VectorDrawable`/Adaptive Icon 规范重绘：

- 背景必须是纯色 `#0F5B85`，禁止现有渐变。
- 前景使用白色、`#79C7E3` 和 `#218657`。
- 所有关键图形放在 Adaptive Icon 中央安全区，建议不超过画布中心 `60%`。
- 四个取景角保持几何一致；绿色勾选可覆盖镜头外缘，但不能破坏或切断右下取景角。
- 线条粗细统一，48px 桌面尺寸仍可清晰辨认。
- `ic_launcher.xml` 和 `ic_launcher_round.xml` 使用相同 foreground/background。
- Android 13 增加单色 `monochrome` 图层。
- Android 8 以下补齐 legacy mipmap 图标。
- 不加入文字、字母、齿轮、人眼、阴影、3D、高光或白色外框。

在真机上分别检查系统圆形和圆角方形遮罩，确认没有裁切、白边、前景过小或图标刷新失败。必要时卸载旧 App 后重新安装验证。

## 8. 通用交互与状态

三个一级页统一实现：

- 正常、首次进入、空数据、加载和错误状态
- 相机权限拒绝和永久拒绝
- 模板缺失、ROI 缺失、图片文件缺失
- 离线、数据库异常和操作失败

规则：

- 非阻塞反馈使用 Snackbar。
- 权限、删除、清理数据和未保存退出使用 Dialog 或 BottomSheet。
- 操作超过 500ms 才显示明显进度。
- 保存期间禁用重复点击，失败时保留用户输入。
- 一级 Tab 保留状态；二级页返回上一层。
- 状态不能只靠 Toast 或颜色表达。
- 不在 UI 中展示调试说明、快捷键教学或未实现功能介绍。

## 9. 验证

每完成一个小任务立即构建，不要等全部改完后统一修编译错误。

完成后执行：

1. `./gradlew :app:testDebugUnitTest --no-daemon`
2. `./gradlew :app:assembleDebug --no-daemon`
3. `./gradlew connectedAndroidTest --no-daemon`
4. 真机冷启动 10 次并检查 logcat 无 `FATAL EXCEPTION`
5. 真机逐一打开现场采集、追溯记录、我的及模板配置、应用设置二级页
6. 验证现场采集没有模板编辑入口
7. 验证模板缺失链接可跳转到“我的 > 模板配置”
8. 验证返回键、Tab 状态恢复、后台恢复和进程重建
9. 验证新版图标在桌面、最近任务和系统应用信息页正确显示

截图验收：

- 现场采集正常状态
- 现场采集模板缺失状态
- 追溯记录空状态
- 我的首屏，模板配置入口必须可见
- 模板列表空状态
- 三个底部 Tab 的选中状态
- 新版桌面图标

尺寸和字体：

- `360x800`
- `412x915`
- `1080x2400`
- 系统字体比例 `1.0`
- 系统字体比例 `1.3`

不得出现文字截断、控件重叠、底部导航遮挡内容、横向溢出、低对比度文字或动态内容导致的布局跳动。

## 10. 完成后汇报

报告：

- 实际修改文件
- 4 Tab 到 3 Tab 的路由迁移结果
- 旧页面/代码的保留与复用方式
- 三个一级页和二级页的截图路径
- 图标资源及真机桌面截图路径
- 单元测试、构建和 `connectedAndroidTest` 实际结果
- 冷启动和 logcat 结果
- APK 路径、大小和 SHA-256
- 尚未实现且明确属于阶段 B/C/D 的内容

更新 `PHASE_A_SUMMARY.md` 和 `PHASE_A_REPORT.md` 时，只记录实际完成和验证过的结果，删除“4 Tab”“暗色主题”“上实时下模板”等已经失效的描述。

完成本次信息架构与视觉重构后暂停，等待用户确认，不要开始阶段 B。
