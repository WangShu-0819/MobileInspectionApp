# 手机端通用视觉检测工作台：Agent 实施指令

## 1. 你的角色

你是一名资深 Android、计算机视觉和工业软件工程师。请把当前 `Wearable Inspection` 工程作为只读的功能和代码迁移来源，在当前工作区内**额外创建一个独立的新 Android 工程** `MobileInspectionApp/`，实现一个仅使用 Android 手机后置摄像头的工业视觉检测 App。新工程必须拥有自己的 `settings.gradle.kts`、根 `build.gradle.kts`、Gradle Wrapper、`app` 模块、资源、数据库和测试，能够独立打开、构建、安装和维护；不得把旧 `app` 模块作为运行时或编译依赖。第一阶段的目标不是追求极致检测准确率，而是交付一个现场人员能够完整演示和试用的闭环：

`建立零件/模板 -> 拍摄模板 -> 绘制 ROI -> 配置检测项 -> 实时轮廓引导 -> 手机拍照 -> ROI 算法检测 -> 查看结果 -> 留存记录 -> 导出完整结果包`

请直接阅读现有代码、制定分步计划、实施、测试并在真机或模拟器上验证。不要只输出设计稿或伪代码。除非确实被外部条件阻塞，否则持续工作到 MVP 可构建、可运行、可演示。

## 2. 当前项目事实与迁移原则

当前旧工程已经是 Android 原生项目，是新工程的迁移参考，主要技术栈为：

- Kotlin、Jetpack Compose、Material 3、Navigation Compose
- CameraX（手机相机和图像分析）
- Room、Kotlin Coroutines/Flow
- OpenCV 4.x
- ML Kit Barcode、ML Kit Text Recognition、ZXing
- 现有包名：`com.wearable.inspection`
- `minSdk 26`、`targetSdk 35`、Java 17

现有项目已有可迁移资产：

- CameraX 手机拍照、实时帧分析、对焦、变焦、闪光灯和预览
- DPM/Data Matrix 扫码及其工业图像预处理、ZXing 兜底
- 钢印 OCR、ROI 裁剪、预处理、结果确认和留档
- 零件、视角、模板样本、匹配结果、采集记录的 Room 数据层
- 模板样本拍摄、相册导入、模板包导入/导出
- OpenCV 特征匹配、模板缓存、自动 ROI、模糊检测
- 记录浏览、图片查看、结果 ZIP 打包

迁移时遵守以下原则：

1. 优先复用已经测试过的 CameraX、DPM、OCR、模板存储和 OpenCV 代码，不重新实现同一能力。
2. 新代码只写入 `MobileInspectionApp/`；未经用户另行授权，不修改、移动或删除旧工程的 `app/`、Gradle 配置、SDK/AAR、数据库和现有功能，旧工程必须继续保持原样可构建。
3. 本版本不使用 AR 眼镜 G40、Leion Pro 或 USB 相机。新工程的 UI、依赖和运行时中不得出现手机/USB/Leion 视频源切换、眼镜连接状态、HUD 设置或 Leion SDK/AAR。
4. 迁移的是功能、必要源码与测试，不是直接依赖旧模块。复制代码时解除设备耦合、调整到新命名空间，并记录来源和必要改动。
5. 新工程使用独立 `applicationId`（建议 `com.wearable.inspection.mobile`）和独立 Room 数据库，因此不得假设能直接读取旧 App 私有数据库。需要迁移的旧零件、模板、DPM 绑定和配置通过现有模板包或新增的显式数据导入流程完成；导入必须校验版本、文件完整性和重复数据。新工程后续自身的 Room schema 变更必须提供 migration，禁止 destructive migration。
6. 先做稳定、可理解、可扩展的检测流程。孔、螺钉、螺纹、小部件检测的准确率和参数后续迭代，第一版必须把算法做成插件式接口，不能把逻辑写死在页面或 ViewModel。

## 3. 产品定位与 UI 信息架构

这是供产线/质检人员反复操作的“检测工作台”，不是营销页。界面应简洁、克制、清晰，适合单手和戴手套操作，参考主流工业视觉、设备运维和质检 App 的工作台布局。整体采用参考图所示的明亮工业台账风格，不再使用大面积暗色背景。

视觉色彩以浅灰蓝页面背景、白色内容面、深蓝主操作和少量青蓝强调为主，同时保留绿色、红色、黄色分别表达通过、失败和待复核，避免整页只有一种蓝色。建议以以下色值作为设计起点，并根据真机对比度微调：

- 页面背景：`#F3F7FA`
- 内容表面：`#FFFFFF`
- 顶部浅色信息带：`#E7F3F8`
- 主色/选中态：`#0F5B85`
- 深色强调：`#0A466B`
- 分隔线/描边：`#D8E3E9`
- 主文字：`#202A33`
- 次级文字：`#74808A`
- 通过：`#218657`
- 不通过：`#C44747`
- 待复核：`#B7791F`

禁止使用渐变、发光、装饰性光斑和大面积纯深蓝背景。主要页面保持充足留白和清晰分组；卡片圆角不超过 8dp，阴影轻微，优先使用边框和背景层级。标题、状态和主操作需要一眼可扫读，说明文字保持短句。

### 3.1 底部一级导航

使用 3 个一级入口，顺序固定：

1. `现场采集`
2. `追溯记录`
3. `我的`

底部导航使用浅灰蓝底色，选中项使用深蓝图标和文字，并可带一个低对比度浅蓝选中背景；未选中项使用灰色。图标使用 Material Icons：现场采集使用相机/取景图标，追溯记录使用历史/列表图标，我的使用用户图标。普通操作的触控区域至少 48dp。所有中文文本在常见手机宽度下不得截断或重叠。

### 3.2 现场采集

`现场采集` 是默认启动页，只承载现场人员高频使用的采集和检测流程，不在此页编辑模板。核心布局必须保持“上实时、下模板”：上半区显示手机相机实时画面并投影模板轮廓，下半区显示当前模板的只读参考图和检测准备信息。首屏按操作顺序显示：

- 顶部标题、当前班次/离线状态等简短环境信息。
- 当前零件选择区：零件名称、型号、DPM 状态，以及扫码切件、钢印 OCR 和切换零件操作。
- 上半区 `实时画面`：CameraX 手机后置摄像头预览，叠加当前模板提取的主体轮廓、ROI 边界和对齐引导；预览区域是页面最大的视觉区域。
- 下半区 `当前模板`：显示模板参考图、模板名称、主体轮廓、ROI 数量和就绪状态，仅供对照，不提供编辑操作。
- 检测操作区：显示相机和模板就绪状态，以及醒目的拍照/开始检测按钮。
- 今日概览：通过、不通过、待复核数量，采用紧凑横向指标，不做大面积统计卡片。
- 最近一次检测：时间、零件、状态和结果图缩略图，点击进入追溯详情。

页面顶部提供当前零件选择器，并保留：

- DPM 扫码选择/切换零件
- 钢印 OCR 识别入口
- 新建、编辑零件入口

无零件、无模板、无 ROI、相机权限拒绝、正在加载、导出中、算法异常都必须有明确的空状态或错误状态，不能只靠 Toast。

现场采集页必须显示当前模板参考图及其轮廓，但不提供“管理模板”或编辑按钮。模板缺失或配置无效时，下半区显示明确的不可检测原因和模板空状态，并提供一个文字链接“前往我的 > 模板配置”；点击后切换到“我的”并定位模板配置入口。

### 3.3 追溯记录

`追溯记录` 统一承载历史检测与结果移交：

- 顶部显示今日/本周通过、不通过、待复核的紧凑统计。
- 支持按零件、模板、检测状态和时间范围筛选，并提供关键字搜索。
- 记录列表显示时间、零件、模板、最终状态和原始图缩略图，点击进入结果详情。
- 结果详情支持原图/识别图查看、ROI 明细、人工复核、备注和单条结果包导出。
- 批量选择或当前筛选结果支持导出完整 ZIP 结果包。
- 空记录、筛选无结果、图片缺失和导出失败均有明确页面状态。

### 3.4 我的

`我的` 采用参考图的账户/台账式纵向分组，但 MVP 不要求登录。顶部显示 App 图标、名称“视觉质检”和“离线可用”，下面按功能分组：

- `模板配置`：作为“我的”页首个核心管理入口，显示零件数、模板数、ROI 数和待完善数量；点击进入零件模板列表，再进入模板拍摄/导入、轮廓提取和 ROI 编辑。
- `基础设置`：相机、拍照质量、提示音/振动、默认检测行为。
- `数据管理`：模板包导入/导出、结果包管理、存储空间与数据清理。
- `权限与诊断`：相机/通知权限状态、版本信息、算法版本和诊断入口。
- `关于`：版本号、构建时间和本地数据说明。

模板、设置和数据管理均不占用底部一级导航。模板配置入口应位于首屏可见区域，不能埋在页面底部；每一项使用图标、标题、简短状态或摘要及右侧箭头。危险的数据清理操作必须二次确认。

### 3.5 实时检测页

实时检测页以 CameraX 全屏预览为主体，覆盖层从模板中提取并投影以下内容：

- 模板主体轮廓，作为手机拍照对齐引导
- 已配置 ROI 的矩形或多边形边界
- 当前目标 ROI 名称和顺序
- 对齐反馈：距离/尺度、左右、上下、旋转方向，以及“未对齐/可拍摄”状态

控制区只保留现场需要的动作：返回、闪光灯、变焦、DPM/OCR 快捷入口、拍照。主快门固定在底部，避免与覆盖层重叠。

对齐流程：

1. 读取当前模板的主体轮廓和基准尺寸。
2. 在实时帧中用已有特征匹配/单应性估计模板姿态。
3. 将模板轮廓和 ROI 通过同一变换投影到预览坐标系。
4. 根据中心偏移、尺度差、旋转角和单应性有效性给出引导。
5. 达到宽松阈值时显示绿色“已对齐，可拍摄”；第一版允许用户在未对齐时手动拍照，但需二次确认，并在结果中记录 `alignmentOverride=true`。
6. 拍照后冻结该张原始图，进入检测执行页；不要对未经用户确认的新帧自动替换。

注意正确处理 CameraX 的传感器旋转、裁剪、PreviewView scale type、前后缓冲尺寸和屏幕坐标映射。轮廓与 ROI 在横竖屏/不同分辨率下必须保持与图像内容一致，不允许简单按屏幕宽高拉伸。

### 3.6 模板配置页

模板配置采用“零件 -> 模板/视角 -> ROI 检测项”的层级。一个零件可以有多个检测模板或视角；每个模板可以配置多个 ROI。

保留并迁移以下能力：

- 手机拍摄模板样本
- 从系统相册/文件导入模板样本
- 模板包导入/导出
- 样本缩略图查看、删除、重拍
- DPM 码绑定零件
- 钢印 OCR 拍照识别和人工确认

新增模板编辑流程：

1. 选择一张主模板图。
2. 自动提取主体轮廓，并允许调整阈值、反相、平滑程度；可一键重置。
3. 在图片上新增 ROI。MVP 至少支持可拖动、可缩放的矩形 ROI；数据模型为未来多边形 ROI 预留类型和点集。
4. ROI 必须存归一化坐标，旋转图片后仍能正确换算。
5. 为 ROI 设置名称、检测类型、期望状态、阈值和可选预处理。
6. 提供 ROI 复制、删除、排序、启用/停用和预览检测。
7. 保存前校验 ROI 不为空、不越界、名称明确且算法参数合法。

ROI 编辑画面应支持双指缩放和平移；编辑控制柄要足够大，不被图片缩放影响实际触控尺寸。屏幕上实时显示选中 ROI 的编号和名称，不显示冗长教学文字。

### 3.7 拍照后检测与结果页

拍照后按模板中启用的 ROI 顺序运行检测。处理过程显示总进度和当前 ROI，不阻塞主线程。单个算法失败不得导致整次会话崩溃，应把该 ROI 标为 `ERROR` 并继续其他 ROI。

结果页需要展示：

- 总状态：`通过 / 不通过 / 需复核 / 执行异常`
- 原始照片
- 识别结果图：在原图上叠加轮廓、ROI 和绿/红/黄状态
- 每个 ROI 的名称、检测类型、期望值、实际状态、分数/置信度、耗时
- 点击 ROI 可查看裁剪前后图、预处理图、算法说明和失败原因
- `重新拍摄`、`保存记录`、`导出` 操作
- 允许人工复核并修改最终状态，但必须同时保留算法原始状态、操作者备注和修改时间

### 3.8 统一设计系统与组件规范

先在 Compose Theme 中建立统一 Token，页面不得各自硬编码颜色和尺寸：

- 页面水平边距：手机统一 `16dp`，宽屏紧凑布局最大内容宽度 `720dp` 并居中。
- 页面区块间距：`20dp`；同组条目间距：`8dp`；组件内部常用间距：`8/12/16dp`。
- 顶部应用栏：`56dp`，一级页显示页面标题和必要操作，二级页显示返回箭头和当前对象名称。
- 底部导航：内容高度约 `72dp` 并适配系统导航栏；三个入口等宽，切换时布局不得跳动。
- 卡片：圆角 `8dp`、`1dp` 浅灰蓝边框，默认白底；只为单个业务对象或入口使用，不把整个页面区块包成漂浮大卡片，也不嵌套卡片。
- 按钮：主按钮使用深蓝实心，次按钮使用白底深蓝描边，危险按钮使用红色；高度至少 `48dp`，禁用态仍需清晰可辨。
- 输入框与筛选控件：高度至少 `48dp`，边框和焦点状态一致；选项较少使用分段控件，选项较多使用菜单或底部弹层。
- 图标按钮：使用 Material Icons 的熟悉图标，至少 `48dp` 触控区域；无文字图标必须提供 `contentDescription` 和 Tooltip。
- 状态标识：使用图标、文字和颜色共同表达，不能只靠颜色。通过为绿色勾选，不通过为红色警示，待复核为黄色时钟/提示，异常为红色错误图标。
- 缩略图：统一 `72dp` 或 `88dp` 方形、`6dp` 圆角、`ContentScale.Crop`；图片缺失显示明确占位图标，不能留下空白框。
- 分隔线：使用 `#D8E3E9` 或主题 outlineVariant，避免大阴影和多层边框。

字体层级保持克制：

- 页面标题：`22sp`、SemiBold。
- 区块标题：`18sp`、SemiBold。
- 卡片/条目标题：`16sp`、Medium 或 SemiBold。
- 正文：`14sp`。
- 辅助说明：`12sp`，但不得低于可读对比度。
- 数字统计：`20sp`、SemiBold，只用于真正重要的数量或状态。

不使用随屏幕宽度缩放的字体，不使用负字距。所有文字支持系统字体放大至 1.3 倍且不重叠；中文标题最多两行，按钮文案优先一行，必要时增加按钮宽度而不是缩小字体。

### 3.9 三大页面的具体布局

#### 现场采集

- 顶部应用栏标题为“现场采集”，右侧只放扫码和钢印 OCR 两个快捷图标；不要再显示冗余欢迎语。
- 标题下使用一条浅蓝信息带显示“离线可用”和当前相机/权限状态，正常时保持低视觉权重，异常时可点击处理。
- 当前零件采用白色整行选择器：左侧零件图标，中间名称和型号，右侧切换箭头；DPM 绑定状态作为次级文字，不做独立大卡片。
- 页面主体使用稳定的上下结构：上方实时相机预览约占可用内容高度的 `55%~65%`，下方当前模板与检测操作约占 `35%~45%`；小屏允许整体纵向滚动，但实时预览不得被压缩成小缩略图。
- 实时预览叠加层绘制模板主体轮廓和 ROI。未完成姿态匹配时使用低饱和白/青色；需要移动时显示简短方向提示；达到阈值后轮廓变绿并显示“已对齐，可拍摄”。
- 下方模板区显示当前模板真实参考图、名称、轮廓预览、ROI 数量和就绪状态。参考图可点击放大查看，但不能从这里编辑或更换 ROI。
- 相机帧上的投影轮廓和下方模板参考图必须来自同一个已选模板及同一份轮廓/ROI 数据，切换零件或模板时二者原子更新，禁止短暂混用旧数据。
- 缺少配置时下方显示模板空状态，拍照/检测按钮禁用，并显示“前往我的 > 模板配置”链接。
- 拍照/开始检测是页面唯一实心主按钮，位于拇指容易触达的位置，不与其他按钮竞争视觉层级。
- 今日概览采用三列紧凑指标，并保持固定高度；数值变化不得引起布局位移。
- 最近结果采用单条记录行，左侧缩略图、中间零件/时间、右侧状态与箭头。没有历史记录时显示简洁空状态。
- 页面内容不足一屏时保持自然顶部排列，不将卡片强行拉伸填满。

#### 追溯记录

- 顶部标题为“追溯记录”，右侧提供搜索；筛选按钮显示当前生效条件数量。
- 状态统计作为一条无嵌套的横向概览区，不使用四个独立大卡片。
- 日期、零件、模板和状态筛选放入 ModalBottomSheet；常用状态可在列表上方使用紧凑 FilterChip。
- 记录按日期分组，分组标题吸顶。每条记录保持固定信息顺序：缩略图、零件名称/模板、检测时间、最终状态、进入箭头。
- 左滑删除不是唯一入口；删除历史必须在详情或多选操作栏中明确触发并二次确认。
- 多选模式使用顶部操作栏显示已选数量、导出和取消，不让底部导航与批量操作同时争夺空间。
- 结果详情优先展示总状态和原图/识别图切换，再展示 ROI 明细；原图和识别图使用标签页或分段控件，不并排挤压手机屏幕。
- ROI 明细按检测顺序排列，可展开查看指标和预处理图；默认收起低频调试字段。

#### 我的

- 顶部为紧凑品牌头：圆角自适应 App 图标、名称“视觉质检”、副标题“现场工作台 · 离线可用”。不显示虚构账号、头像、角色或云同步状态。
- 首个区块标题为“检测配置”，第一项必须是“模板配置”。副标题动态显示“X 个零件 · X 个模板 · X 个 ROI”，存在缺失配置时以黄色文字显示待完善数量。
- “模板配置”后可放“零件管理”和“默认检测参数”，三项使用统一列表行，不做三张尺寸不一致的大卡片。
- 第二个区块为“数据管理”：模板包导入/导出、结果包管理、存储空间。
- 第三个区块为“应用设置”：相机与图像质量、提示音与振动、权限与诊断。
- 页面底部以次级文字显示版本号、构建日期和“数据仅保存在本机”，不放在卡片中。
- 清理数据等危险操作使用红色文字并置于二级页面，不能与常用入口紧邻。

### 3.10 二级页面与交互细节

- 模板列表按零件筛选；每个模板条目显示真实样本缩略图、名称、ROI 数、更新时间和启用状态。页面右下角使用带相机/添加图标的扩展 FAB 新建模板，滚动时可收起为圆形图标。
- 新建模板采用明确的步骤状态：选择零件、拍摄/导入、提取轮廓、配置 ROI、确认保存。使用顶部步骤指示或短标题，不用说明性大卡片占据屏幕。
- ROI 编辑器以图片画布为主，工具栏固定且不遮挡图片；添加、删除、复制、撤销/重做使用图标按钮，检测类型和阈值使用底部属性面板。
- 所有保存动作在执行中禁用重复点击；成功后返回上一层并立即显示新数据，失败时保留用户当前编辑内容。
- Snackbar 用于非阻塞反馈；权限拒绝、删除确认、未保存退出和强制未对齐拍照使用 Dialog 或 BottomSheet。
- 页面切换使用克制的系统级过渡；列表增删可以短暂淡入淡出，不使用弹跳、旋转或持续动画。
- 导航返回必须符合 Android 习惯：二级页返回上一层，一级页返回退出；切换底部 Tab 后保留各自滚动和筛选状态。
- 加载时优先显示与真实布局接近的骨架或局部进度，不用全屏转圈遮住可用内容；操作超过 500ms 才显示明显进度。

### 3.11 UI 验收标准

- 以参考图的明亮灰蓝/白色风格为方向，但不得照搬其中“电子台账”、人员/工房等与本产品无关的内容。
- 三个一级页使用同一顶栏、边距、字体、卡片、列表和状态样式；不存在页面之间明显像不同 App 的情况。
- 首屏不出现多层卡片、超大标题、横向溢出、按钮拥挤、状态栏遮挡或底部导航覆盖内容。
- 现场采集页在 360x800 屏幕上同时能辨认上方实时画面、下方当前模板和主拍照操作；“我的”页的模板配置入口在首屏可见。
- 浅色背景上的正文、次级文字、边框和禁用态满足可读对比度；状态不能只用红绿区分。
- 对正常、空数据、加载、权限拒绝、离线、检测失败和文件缺失状态分别截图验收。
- 使用 Compose Preview 和真机截图检查 360x800、412x915、1080x2400，以及系统字体 1.0/1.3 倍；不得有裁切、重叠或布局跳动。
- 实现后逐页对照本节清单，不以“功能可点击”替代视觉与交互验收。

## 4. 通用 ROI 与算法插件设计

建立稳定的领域接口，名称可结合现有代码风格调整，但职责不得混入 UI：

```kotlin
interface RoiInspectionAlgorithm {
    val type: InspectionType
    suspend fun inspect(input: RoiInspectionInput): RoiInspectionResult
}

data class RoiInspectionInput(
    val originalImage: Mat,
    val roiImage: Mat,
    val templateRoiImage: Mat?,
    val config: RoiInspectionConfig,
)

data class RoiInspectionResult(
    val status: InspectionStatus,
    val score: Float?,
    val message: String,
    val metrics: Map<String, Double>,
    val debugImages: List<DebugImageRef>,
    val durationMs: Long,
)
```

必须具备：

- `InspectionType`：`PRESENCE_GENERIC`、`HOLE_PRESENCE`、`SCREW_PRESENCE`、`THREAD_PRESENCE`、`SMALL_PART_PRESENCE`，并保留扩展能力。
- `InspectionStatus`：`PASS`、`FAIL`、`REVIEW`、`ERROR`、`SKIPPED`。
- 算法注册表/工厂：根据类型选择实现，页面不使用 `when` 直接调用具体 OpenCV 类。
- 统一预处理流水线：灰度、CLAHE、去噪、二值化、反相、边缘增强、形态学操作；每个 ROI 只启用配置需要的步骤。
- 所有 OpenCV `Mat` 的所有权和释放规则必须明确，异常路径也不能泄漏。

MVP 算法要求：

1. 通用有无检测作为默认算法：比较模板 ROI 和实拍 ROI 的结构/边缘/特征相似度，输出可调阈值和 `PASS/FAIL/REVIEW`。
2. 孔、螺钉、小部件第一版可基于通用有无检测加少量轮廓、圆形或边缘规则，重点是走通独立类型和参数配置。
3. 螺纹检测必须有独立的预处理配置和实现入口；MVP 可使用方向性梯度、边缘密度或频域/线特征的基础指标。证据不足时返回 `REVIEW`，禁止伪造高置信度结果。
4. 每个算法提供纯 JVM 可测的核心逻辑，至少包含正常、缺失、低质量/模糊和非法 ROI 用例。

## 5. 数据模型与持久化

在复用现有实体的基础上补齐以下概念，具体表名遵循仓库命名规范：

- `InspectionTemplate`：零件、名称/视角、主图、主体轮廓、创建/更新时间、启用状态。
- `RoiDefinition`：模板、名称、顺序、形状类型、归一化坐标/点集、检测类型、期望值、参数 JSON、预处理 JSON、启用状态。
- `InspectionSession`：零件、模板、原始图片、标注结果图片、开始/结束时间、自动总状态、最终总状态、对齐质量、是否强制拍照、备注。
- `RoiInspectionRecord`：会话、ROI 快照、算法类型/版本、自动状态、最终状态、分数、指标 JSON、耗时、ROI 裁剪图、预处理图、错误信息。

记录必须保存“配置快照”，不能因为用户后来修改 ROI 或阈值而改变历史结果的解释。图片写入 App 私有目录，Room 仅保存路径和元数据；采用稳定文件命名并清理失败任务产生的孤儿文件。

## 6. 完整结果包与 Excel 导出

导出的主交付物不是单独一个 Excel，而是一个可追溯、可离线移交的结果包 `.zip`。通过 Android Storage Access Framework 让用户选择保存位置或系统分享。打包在 IO 线程执行，有进度、取消、成功和失败反馈；失败或取消时清理未完成的临时文件。

结果包采用稳定目录结构，至少包含：

```text
inspection_<part>_<timestamp>.zip
├── report.xlsx
├── manifest.json
├── images/
│   ├── original/
│   ├── annotated/
│   ├── roi/
│   └── preprocessed/
├── config/
│   ├── template_snapshot.json
│   └── roi_config_snapshot.json
└── logs/
    └── errors.json
```

- `report.xlsx`：便于人员查看、筛选和汇总。
- `manifest.json`：包版本、App/算法版本、导出时间、记录与文件的稳定 ID、相对路径、文件大小及 SHA-256，便于程序再次解析和完整性校验。
- `images/original`：未经标注的原始拍照文件。
- `images/annotated`：叠加轮廓、ROI 与检测状态的识别图。
- `images/roi`、`images/preprocessed`：各 ROI 的裁剪图和实际使用的预处理图；没有生成的文件不创建空引用。
- `config`：本次记录使用的模板轮廓、ROI、阈值、预处理和算法版本快照，保证结果可复现。
- `logs/errors.json`：仅保存本次检测与导出相关的结构化错误和耗时，不得包含无关设备隐私或敏感系统日志。

结果包内所有引用使用相对路径，文件名必须经过安全化处理且不依赖中文文件系统行为。打包前检查每个引用文件存在；缺失文件写入 `manifest.json` 的 warnings，不能静默忽略。ZIP 内不得包含 App 数据库、用户未选择的历史记录或与本次导出无关的图片。

同时实现真正的 `.xlsx` 报表，并允许用户选择“导出当前记录”或“按筛选条件批量导出”。Excel 是结果包的一部分，不替代原始文件和配置快照。

工作簿至少包含：

- `检测汇总`：记录编号、零件名称/型号、DPM 码、模板、检测时间、自动状态、最终状态、对齐质量、原始图、识别图、备注。
- `ROI 明细`：记录编号、ROI 名称、检测类型、期望状态、算法状态、最终状态、分数、核心指标、耗时、错误信息、ROI 图/预处理图。

图片应以缩略图嵌入单元格并保持纵横比，同时填写其在结果包中的相对路径；完整分辨率图片保存在 `images/` 中。控制单元格尺寸和缩略图压缩，避免同一完整图片在 Excel 与 ZIP 中重复占用大量空间。若 Android 兼容性或内存限制使某个 Excel 库不可用，先做最小原型验证再选库，不要把未经验证的重型依赖直接引入新工程。

至少验证：ZIP 可正常解压且无路径穿越风险；manifest 与实际文件一一对应且校验值正确；中文不乱码；Excel/WPS 可打开；缩略图可见；完整图片可从相对路径找到；筛选/冻结表头可用；空字段合法；100 条记录及对应图片导出不会 OOM。

## 7. 架构与工程约束

- 保持单向数据流：Compose Screen -> ViewModel -> Repository/UseCase -> DAO/算法。
- ViewModel 只暴露不可变 `StateFlow` UI state 和清晰的用户 action。
- Camera 帧处理、OpenCV 运算、文件保存、Excel 生成和结果包压缩都离开主线程。
- 每个实时帧必须及时关闭 `ImageProxy`；分析使用背压/节流，避免队列堆积和内存暴涨。
- 拍照检测和实时对齐共享变换/坐标模型，但不得争用同一个可变 `Mat`。
- 不新增后端依赖，MVP 离线可用；不得要求登录或联网。
- 不将算法阈值散落在 Composable 中；通过配置对象和设置页/ROI 编辑器管理。
- 不为未来需求过度搭建框架。只实现当前闭环需要的扩展点。
- 新增代码遵循现有包结构和命名方式；复杂功能拆成小文件并配单元测试。

## 8. 实施顺序

开始编码前先输出并保存一份可检查的计划，列出旧工程中拟读取/迁移的来源文件，以及新工程中拟新增的文件；确认工作区已有改动并保护用户改动。除新增 `MobileInspectionApp/` 外不改旧工程。然后按以下纵向切片实施，每一步都保持新旧两个工程各自可构建：

### 阶段 A：基线与手机化

1. 构建并运行当前项目，记录现有测试结果。
2. 在 `MobileInspectionApp/` 创建可独立构建的新 Android 工程，并为新工程确定独立命名空间、`applicationId` 和数据库名。
3. 画出现有 DPM、OCR、模板、CameraX、Room、结果记录的依赖关系，只迁移手机端闭环真正需要的源码和测试。
4. 建立新的 3 Tab 导航（现场采集/追溯记录/我的）；现场采集专注拍照检测，模板配置和设置均作为“我的”模块内的二级页面。
5. 新工程只实现手机后置摄像头，不引入 G40/Leion/USB 代码和依赖。

验收：新旧工程可分别独立构建；新 App 可与旧 App 共存安装并在普通 Android 手机上启动；现场采集、追溯记录、我的三个一级页面可达且选中状态正确；模板配置和设置均可从“我的”进入；现场采集不出现模板编辑入口；DPM、OCR 入口仍可用；不连接眼镜也无错误状态。

### 阶段 B：模板轮廓与 ROI 编辑闭环

1. 在新工程中新增模板/ROI 持久化、初始 Room schema 和面向旧模板/数据包的显式导入器。
2. 完成主模板拍摄/导入、轮廓提取与参数预览。
3. 完成矩形 ROI 新增、拖动、缩放、排序、删除、算法与阈值配置。
4. 完成保存、重新进入、坐标还原和模板预览检测。

验收：用户可从零开始建立一个零件，拍模板，画 3 个不同检测类型 ROI，退出重进后数据和位置完全一致。

### 阶段 C：实时投影、拍照与检测

1. 建立模板坐标、图像坐标和预览坐标的统一变换模型。
2. 将模板主体轮廓和 ROI 实时投影到手机预览。
3. 实现对齐状态、手动快门、冻结原图和检测任务状态机。
4. 接入算法注册表、通用有无检测和各类型 MVP 实现。
5. 生成带 ROI 状态的识别结果图，保存会话和 ROI 明细。

验收：对同一工件能看到随相机姿态移动的引导轮廓；拍照后所有 ROI 都有独立结果；算法异常不会丢失整次记录。

### 阶段 D：记录、完整结果包与功能迁移收口

1. 重做记录列表、筛选、详情和人工复核。
2. 完成包含 `.xlsx`、manifest、原始图、识别图、ROI/预处理图、配置快照和错误信息的标准结果 ZIP 包导出。
3. 完整回归 DPM 扫码切件、钢印 OCR、模板拍摄/相册导入、模板包导入/导出。
4. 检查新工程没有 G40/Leion 入口、SDK、AAR 或运行时依赖；不得通过修改旧工程来完成清理。
5. 补齐错误态、权限、进程重建、低存储空间和导出失败处理。

验收：能够向现场人员完整演示，从选择零件、完成检测、导出 ZIP、核对 manifest 和图片，一直到 WPS/Excel 打开包内报表，全程不需要开发者工具。

## 9. 测试与验证要求

每个阶段至少执行：

- 对改动模块的 JVM 单元测试
- `./gradlew :app:testDebugUnitTest`
- `./gradlew :app:assembleDebug`
- Android 真机或模拟器手动检查

重点测试：

- ROI 归一化、裁剪、旋转和预览坐标映射
- 模板轮廓经单应性投影后的坐标正确性
- 新工程 Room 数据可持续升级，旧模板/数据包可显式导入且失败不污染现有数据
- 算法注册和每个 `InspectionStatus` 分支
- 某个 ROI 算法抛异常时其他 ROI 仍完成
- 原图、结果图、ROI 图的文件生命周期
- 结果 ZIP 目录、manifest、校验值、Excel 字段、中文、图片、空值、大批量记录和内存占用
- 相机权限拒绝/永久拒绝、页面切换、后台再恢复
- DPM、钢印 OCR 和模板导入的原有回归测试

UI 必须在至少 360x800、常见 1080x2400 手机尺寸验证，并检查大字体模式。用 Compose Preview、截图或 UI 测试确认无文本截断、重叠、布局跳动和不可点击区域。

## 10. MVP 完成定义

只有同时满足以下条件才算完成：

- `MobileInspectionApp/` 是可独立打开、构建、测试和安装的新工程；旧工程未被改造或破坏，新旧 App 可以共存。
- 普通 Android 手机可安装并离线运行，完全不依赖 G40/Leion。
- 底部只有现场采集、追溯记录、我的三个一级入口；现场采集保持“上实时、下模板”的只读对齐工作区，模板的拍摄、编辑和 ROI 配置位于“我的”首屏入口。
- 可拍摄/导入模板，提取轮廓，创建并持久化多个通用 ROI。
- 实时画面能投影模板轮廓和 ROI，提供基本对齐引导。
- 手机拍照后能依次运行孔、螺钉、螺纹、小部件/通用有无检测入口。
- 每个 ROI 有状态、分数或指标、耗时和结果图，总结果可人工复核。
- 记录中保存原始图、识别图、自动状态、最终状态和配置快照。
- 可导出包含 `.xlsx`、manifest、原始图、识别图、ROI/预处理图、配置快照和错误信息的完整 ZIP 结果包；Excel/WPS 能正常打开包内报表，程序可依据 manifest 解析全部记录。
- 原有 DPM 扫码、钢印 OCR、模板样本拍摄/导入能力已迁移并通过回归。
- 新工程全量单测和 Debug 构建通过，旧工程仍可按原方式构建；已知准确率局限被记录为后续参数/算法任务，而不是用硬编码掩盖。

## 11. Agent 工作方式与汇报格式

执行期间：

1. 先读代码和现有文档，再提出架构判断；不要凭空假设仓库状态。
2. 每次只推进一个可验收的小任务，修改前说明涉及文件和原因。
3. 发现已有未提交改动时保留并兼容，禁止回滚用户改动。
4. 每完成一个阶段，报告已完成项、实际测试命令及结果、真机验证证据、剩余风险。
5. 遇到算法准确率不足时展示真实指标与失败样本，优先返回 `REVIEW`，禁止针对单张图片写死结果。
6. 最终交付需列出新工程路径、新 APK 路径、独立构建命令、关键入口、旧功能迁移清单、数据导入说明、完整结果包示例路径与内容校验结果、测试结果和仍需迭代的算法参数。

请从仓库审计和阶段 A 开始执行。
